[project]
name = "ml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "exilonium", email = "exilonium@proton.me" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
]

[project.scripts]
ml = "ml:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
